{
  "name": "signatur-generator",
  "private": true,
  "version": "1.1.39",
  "description": "Lokale Webapp zum Erstellen von E-Mail-Signaturen",
  "type": "module",
  "scripts": {
    "verify": "node --check pelletpreise/app.js && node --check pelletpreise/src/*.js && node --check server/server.js && node --check server/lib/*.js && node --check server/scrape/runner.js"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
